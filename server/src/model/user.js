/*
export const database = Array.from({ length: 10 }, (_, index) => {
  return {
    id: index + 1,
    username: `user ${index + 1}`,
    password: `user ${index + 1}`,
    createdAt: new Date(),
  };
});
*/

export const database = [];
