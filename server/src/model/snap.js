/*
export const database = Array.from({ length: 3 }, (_, index) => {
  return {
    id: index + 1,
    snap: `snap ${index + 1}`,
    username: `user ${index + 1}`,
    createdAt: new Date(),
  };
});
*/

export const database = [];
